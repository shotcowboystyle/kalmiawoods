---
---

<script is:inline>
  // function applyTheme(theme, themeToggleEl) {
  //   const root = document.documentElement;
  //   root.setAttribute('data-theme', theme);
  //   themeToggleEl.setAttribute('aria-pressed', isDark ? 'false' : 'true');
  //   themeToggleEl.setAttribute('aria-label', isDark ? 'Enable dark mode' : 'Disable dark mode');
  //   themeToggleEl.classList.toggle('swap-active');
  // }

  function attachEvent(selector, event, fn) {
    const matches = typeof selector === 'string' ? document.querySelectorAll(selector) : selector;
    if (matches?.length) {
      matches.forEach((elem) => {
        elem.addEventListener(event, (e) => fn(e, elem), { passive: true });
      });
    }
  }

  window.onload = function () {
    // const theme = localStorage.getItem('theme');
    // if (theme === 'dark' || (!theme && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
    //   applyTheme('dark');
    // } else {
    //   applyTheme('light');
    // }

    let lastKnownScrollPosition = window.scrollY;
    let ticking = true;

    function appyNavbarStylesOnScroll() {
      const navbar = document.getElementById('navbar');
      if (navbar?.classList.contains('show-booking-quick-form')) {
        navbar.classList.remove('show-booking-quick-form');
      }

      if (lastKnownScrollPosition > 60 && !navbar?.classList.contains('scroll')) {
        navbar.classList.add('scroll');
      } else if (lastKnownScrollPosition <= 60 && navbar?.classList.contains('scroll')) {
        navbar.classList.remove('scroll');
      }
      ticking = false;
    }
    appyNavbarStylesOnScroll();

    // attachEvent('[data-kw-toggle-menu]', 'click', function (_, elem) {
    //   elem.classList.toggle('expanded');
    //   document.body.classList.toggle('overflow-hidden');
    //   document.getElementById('navbar')?.classList.toggle('h-screen');
    //   document.querySelector('#navbar nav')?.classList.toggle('hidden');
    // });

    attachEvent([document], 'scroll', function () {
      lastKnownScrollPosition = window.scrollY;

      if (!ticking) {
        window.requestAnimationFrame(() => {
          appyNavbarStylesOnScroll();
        });
        ticking = true;
      }
    });
  };

  // window.onpageshow = function () {
  //   document.documentElement.classList.add('motion-safe:scroll-smooth');
  //   const elem = document.querySelector('[data-kw-toggle-menu]');
  //   if (elem) {
  //     elem.classList.remove('expanded');
  //   }
  //   document.body.classList.remove('overflow-hidden');
  //   document.getElementById('navbar')?.classList.remove('h-screen');
  //   document.querySelector('#navbar nav')?.classList.add('hidden');
  // };
</script>
