---
// import { auth } from '@/auth/lucia';
// import { emailRegex, isValidFormSubmission } from '@/auth/utils/forms/submission';
import Logo from '@/components/Logo.astro';
// import { DASHBOARD_HOME } from '@/constants';
// import { LuciaError } from 'lucia-auth';

// let errorMessage: string | null = null;
// let email = '';

// const authRequest = auth.handleRequest(Astro);

// const handleFormSubmission = async (): Promise<Response | null> => {
//   const validSubmission = isValidFormSubmission(Astro.request);
//   if (!validSubmission) {
//     return null;
//   }

//   const formData = await Astro.request.formData();
//   email = formData.get('email')?.toString() ?? '';
//   if (email === null || !emailRegex.test(email)) {
//     errorMessage = 'Incorrect email or password';
//     return null;
//   }

//   const password = formData.get('password');
//   if (password instanceof File || password === null) {
//     errorMessage = 'Incorrect email or password';
//     return null;
//   }

//   try {
//     const key = await auth.useKey('email', email, password);
//     const session = await auth.createSession(key.userId);
//     authRequest.setSession(session);
//     return Astro.redirect(DASHBOARD_HOME, 302);
//   } catch (e) {
//     if (e instanceof LuciaError && e.message === 'AUTH_INVALID_KEY_ID') {
//       errorMessage = 'Incorrect email or password';
//       return null;
//     }
//     if (e instanceof LuciaError && e.message === 'AUTH_INVALID_PASSWORD') {
//       errorMessage = 'Incorrect email or password';
//       return null;
//     }
//     errorMessage = 'An unknown error occurred';
//     return null;
//   }
// };

// const formSubmissionResponse = await handleFormSubmission();
// if (formSubmissionResponse) {
//   return formSubmissionResponse;
// }
---

<div
  class="pt:mt-0 mx-auto flex w-full flex-col items-center justify-center px-6 pt-8 dark:bg-gray-900">
  <a
    href="#"
    class="mb-8 flex items-center justify-center text-2xl font-semibold dark:text-white lg:mb-10">
    <Logo alt="Kalmia Woods Logo" class="mr-4 h-11" />
    <span>Kalmia Woods</span>
  </a>
  <!-- Card -->
  <div class="w-full max-w-xl space-y-8 rounded-lg bg-white p-6 shadow dark:bg-gray-800 sm:p-8">
    <h2 class="text-2xl font-bold text-gray-900 dark:text-white">
      Sign in to access private pages
    </h2>
    <!-- {errorMessage && <p class="error">{errorMessage}</p>} -->
    <form class="mt-8 space-y-6" action="/api/auth" method="post">
      <div>
        <label for="email" class="mb-2 block text-sm font-medium text-gray-900 dark:text-white"
          >Your email</label
        >
        <input
          type="email"
          name="email"
          id="email"
          class="focus:ring-primary-500 focus:border-primary-500 dark:focus:ring-primary-500 dark:focus:border-primary-500 block w-full rounded-lg border border-gray-300 bg-gray-50 p-2.5 text-gray-900 dark:border-gray-600 dark:bg-gray-700 dark:text-white dark:placeholder-gray-400 sm:text-sm"
          placeholder="name@company.com"
          required
        />
        <!-- value={email} -->
      </div>
      <div>
        <label for="password" class="mb-2 block text-sm font-medium text-gray-900 dark:text-white"
          >Your password</label
        >
        <input
          type="password"
          name="password"
          id="password"
          placeholder="••••••••"
          class="focus:ring-primary-500 focus:border-primary-500 dark:focus:ring-primary-500 dark:focus:border-primary-500 block w-full rounded-lg border border-gray-300 bg-gray-50 p-2.5 text-gray-900 dark:border-gray-600 dark:bg-gray-700 dark:text-white dark:placeholder-gray-400 sm:text-sm"
          required
        />
      </div>
      <div class="flex flex-wrap items-start">
        <div class="flex h-5 items-center">
          <input
            id="remember"
            aria-describedby="remember"
            name="remember"
            type="checkbox"
            class="focus:ring-3 focus:ring-primary-300 dark:focus:ring-primary-600 h-4 w-4 rounded border-gray-300 bg-gray-50 dark:border-gray-600 dark:bg-gray-700 dark:ring-offset-gray-800"
          />
        </div>
        <div class="ml-3 text-sm">
          <label for="remember" class="font-medium text-gray-900 dark:text-white">
            Remember me
          </label>
        </div>
        <a
          href="/auth/password-reset"
          class="text-primary-700 dark:text-primary-500 link link-primary ml-auto mt-4 w-full text-left text-sm hover:underline">
          Lost Password?
        </a>
      </div>
      <!-- <button
        type="submit"
        class="w-full px-5 py-3 text-base font-medium text-center text-white rounded-lg btn btn-primary btn-full bg-primary-700 hover:bg-primary-800 focus:ring-primary-300 dark:bg-primary-600 dark:hover:bg-primary-700 dark:focus:ring-primary-800 focus:ring-4 sm:w-auto">
        Login to your account
      </button> -->
      <input
        type="submit"
        class="btn btn-primary btn-full bg-primary-700 hover:bg-primary-800 focus:ring-primary-300 dark:bg-primary-600 dark:hover:bg-primary-700 dark:focus:ring-primary-800 w-full rounded-lg px-5 py-3 text-center text-base font-medium text-white focus:ring-4 sm:w-auto"
        value="Login to your account"
      />
      <div class="text-sm font-medium text-gray-500 dark:text-gray-400">
        Not registered? &nbsp;
        <a
          class="text-primary-700 dark:text-primary-500 link link-primary hover:underline"
          href="/signup">
          Create account
        </a>
      </div>
    </form>
  </div>
</div>
