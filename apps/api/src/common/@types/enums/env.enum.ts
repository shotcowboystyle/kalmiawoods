export type EnvType = 'development' | 'testing' | 'staging' | 'production';

// Valid Environment values
export enum EnvEnum {
  Dev = 'development',
  Testing = 'testing',
  Staging = 'staging',
  Prod = 'production',
}
