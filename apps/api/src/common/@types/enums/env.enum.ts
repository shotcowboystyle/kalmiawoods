export type EnvType = 'local' | 'development' | 'staging' | 'testing' | 'production';

// Valid Environment values
export enum EnvEnum {
  Local = 'local',
  Dev = 'development',
  Staging = 'staging',
  Testing = 'testing',
  Prod = 'production',
}
