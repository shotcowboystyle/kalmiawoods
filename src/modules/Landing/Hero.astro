---
import heroBg from '@/assets/images/hero/background.png';
// import heroLogo from '@/assets/images/hero/hero-logo.png';
import SvgSprite from '@/components/SvgSprite.astro';
import { Image } from 'astro:assets';
---

<section id="hero-section">
	<Image
		src={heroBg}
		alt=""
		widths={[640, 828, 2400, heroBg.width]}
		class="absolute inset-0 h-full w-full object-cover"
	/>

	<SvgSprite
		name="home-header-sun"
		class="parallax-layer absolute left-0 top-0 h-full w-full object-cover object-center"
	/>
	<SvgSprite
		name="home-header-clouds"
		class="parallax-layer absolute left-0 top-0 h-full w-full object-cover object-center"
	/>
	<SvgSprite
		name="home-header-mountain-5"
		class="parallax-layer parallax__mountain__layer absolute left-0 top-0 h-full w-full scale-110 transform object-cover object-center"
	/>
	<SvgSprite
		name="home-header-mountain-4"
		class="parallax-layer parallax__mountain__layer absolute left-0 top-0 h-full w-full scale-110 transform object-cover object-center"
	/>
	<SvgSprite
		name="home-header-mountain-3"
		class="parallax-layer parallax__mountain__layer absolute left-0 top-0 h-full w-full scale-110 transform object-cover object-center"
	/>
	<SvgSprite
		name="home-header-mountain-2"
		class="parallax-layer parallax__mountain__layer absolute left-0 top-0 h-full w-full scale-110 transform object-cover object-center"
	/>
	<SvgSprite
		name="home-header-mountain-1"
		class="parallax-layer parallax__mountain__layer absolute left-0 top-0 h-full w-full scale-110 transform object-cover object-center"
	/>
	<SvgSprite
		name="home-header-trees"
		class="parallax-layer absolute left-0 top-0 h-full w-full scale-110 transform object-cover object-center"
	/>
	<SvgSprite
		name="home-header-landscape"
		class="parallax-layer absolute left-0 top-0 h-full w-full scale-110 transform object-cover object-center"
	/>
	<SvgSprite
		name="home-header-forest-3"
		class="parallax-layer parallax__forest__layer absolute left-0 top-0 h-full w-full scale-125 transform object-cover object-center"
	/>
	<SvgSprite
		name="home-header-forest-2"
		class="parallax-layer parallax__forest__layer absolute left-0 top-0 h-full w-full scale-125 transform object-cover object-center"
	/>
	<SvgSprite
		name="home-header-forest-1"
		class="parallax-layer parallax__forest__layer absolute left-0 top-0 h-full w-full scale-125 transform object-cover object-center"
	/>

	<!-- <div
		class="page-header-body absolute inset-0 flex h-full items-center justify-center text-center"
	>
		<div class="page-title-origin relative mb-[5vh] px-[6.25vw] py-0">
			<div
				data-powy="0.85"
				class="page-title js-scroll relative z-[2] [@media(prefers-color-scheme:dark)]:text-base-content"
				data-shown="1"
				data-visible="1"
			>
				<h1 class="h1 font-display font-bold text-9xl">
					<div class="clip flip">
						<span class="o"><span class="t">Kalmia</span></span>
						<span class="o"><span class="t">Woods</span></span>
					</div>
				</h1>

				<p class="lead">
					<div class="clip flip ja">
						<span class="o"><span class="t">Our cabin is located in the heart</span></span>
						<span class="o"
							><span class="t">of the Keowee-Toxaway wilderness of Oconee County.</span></span
						>
					</div>
				</p>
			</div>
		</div>
	</div> -->
</section>

<script>
	import { parallaxScroll } from '@/utils/parallax';
	import { gsap } from 'gsap';
	import { ScrollTrigger } from 'gsap/ScrollTrigger';

	gsap.registerPlugin(ScrollTrigger);

	const parallaxOffset = (index: number) =>
		(3.45 * 50) / (index >= 5 ? index - 0.5 : index >= 2 ? index : 1.75);

	const initHeaderTilt = () => {
		const parallaxContainer = document.getElementById('hero-section');
		if (parallaxContainer) {
			const parallaxImages = gsap.utils.toArray('.parallax-layer');

			parallaxImages.forEach((image, index) => {
				parallaxScroll(image as HTMLElement, parallaxContainer, parallaxOffset(index));
			});
		}
	};

	const init = () => {
		initHeaderTilt();
	};

	init();
</script>

<style is:global>
	#hero-section {
		position: relative;
		height: 100vh;
		width: 100%;
		perspective: 1px;
		perspective-origin: center;
	}

	.parallax-layer {
		transform-style: preserve-3d;
		will-change: transform;
	}

	.lead {
		padding-top: 0.5em;
		font-weight: 600;
		letter-spacing: 0.08em;
		line-height: 1.8;
	}
</style>
