---
import SvgSprite from '@/components/SvgSprite.astro';

const { class: className, ...rest } = Astro.props;
---

<SvgSprite
	name="kalmia-woods-logo"
	aria-label={import.meta.env.APP_NAME}
	class={className}
	{...rest}
/>
