---
type Props = {
	id: string;
	message?: string;
};

const { id, message } = Astro.props;
---

{
	message && (
		<p
			id={id}
			class="text-error mt-1 text-sm"
			role="alert"
		>
			{message}
		</p>
	)
}
