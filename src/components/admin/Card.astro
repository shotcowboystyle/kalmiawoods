---
type Props = {
	class?: string;
	padding?: boolean;
};

const { class: className = '', padding = true } = Astro.props;
---

<div
	class:list={[
		'bg-base-100 border-base-300 rounded-xl border shadow-sm',
		padding && 'p-6',
		className,
	]}
>
	<slot />
</div>
