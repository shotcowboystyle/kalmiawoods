---
export interface props {
	name: string;
}

const { class: className, name, ...rest } = Astro.props;
---

<svg
	class={className}
	{...rest}
>
	<use href={`/assets/images/sprite.svg#${name}`}></use>
</svg>
