{
  "name": "@kalmiawoods/database",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json --tsBuildInfoFile null",
    "build:watch": "tsc -p tsconfig.build.json --tsBuildInfoFile null -w --preserveWatchOutput",
    "db:generate": "prisma generate",
    "db:migrate:deploy": "prisma migrate deploy --preview-feature",
    "db:migrate:dev": "prisma migrate dev --preview-feature",
    "db:migrate:dev:create": "prisma migrate dev --create-only --preview-feature",
    "db:migrate:reset": "prisma migrate reset --preview-feature",
    "db:migrate:resolve": "prisma migrate resolve --preview-feature",
    "db:migrate:status": "prisma migrate status --preview-feature",
    "db:push": "prisma db push --skip-generate",
    "db:pull": "pnpm dlx prisma db pull",
    "db:seed": "prisma db seed",
    "db:docs:serve": "pnpm dlx prisma-docs-generator serve",
    "dev": "pnpm run build:watch",
    "postinstall": "run-s db:generate build",
    "prebuild": "rimraf dist",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^4.15.0"
  },
  "devDependencies": {
    "dotenv": "^16.1.4",
    "prisma": "4.15.0",
    "prisma-docs-generator": "^0.8.0",
    "prisma-erd-generator": "^1.6.2"
  },
  "peerDependencies": {
    "@nestjs/common": "^9.3.12",
    "dotenv": "^16.1.4",
    "nestjs-pino": "^3.1.2"
  },
  "prisma": {
    "schema": "./prisma/schema.prisma",
    "seed": "ts-node ./prisma/seed.ts"
  }
}
